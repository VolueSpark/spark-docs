# Authentication

As of now, the only authentication needed is a valid API key. This can be acquired by emailing us at example@somethingsomething. The key should be kept secret on your server and is required with every request made to the API. They should not be shared outside your organization.
